spring:
  application:
    name: catalog-service
  cloud:
    config:
      fail-fast: true
      discovery:
        enabled: true
        service-id: config-server
  profiles:
    active: dev

eureka:
  client:
    service-url:
      defaultZone: http://eureka-service:8761/eureka